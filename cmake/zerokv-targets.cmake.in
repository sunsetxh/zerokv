# ZeroKV CMake targets
# Generated by CMake

if(TARGET zerokv::zerokv)
    return()
endif()

add_library(zerokv::zerokv STATIC IMPORTED)

# Set the import location
set_target_properties(zerokv::zerokv PROPERTIES
    IMPORTED_LOCATION "${CMAKE_CURRENT_LIST_DIR}/@CMAKE_INSTALL_LIBDIR@/libzerokv.a"
    INTERFACE_INCLUDE_DIRECTORIES "${CMAKE_CURRENT_LIST_DIR}/@CMAKE_INSTALL_INCLUDEDIR@"
)

# Link dependencies
if(USE_UCX)
    set_target_properties(zerokv::zerokv PROPERTIES
        INTERFACE_LINK_LIBRARIES "ucp;ucs;uct"
    )
endif()
